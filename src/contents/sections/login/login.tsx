import {ReactElement} from "react";

function Login(): ReactElement {
    return <section className={"h-screen w-screen flex justify-center items-center bg-secondary"}>
        test
    </section>
}

export default Login;